
   Problem 1 (Wind Chill)
      Approach: set t and v to command line argument. Used the given formula. followed the directions. Got the output.

      Issues and resolution: no issues

   Problem 2 (Day of the Week)
      Approach: set variable as command line argument. Used the formula given. Got the output desired.

      Issues and resolution: no issues

   Problem 3 (Playing Card)
      Approach: set the variables. Used if statements .Got the desired output.

      Issues and resolution: no issues

   Problem 4 (Root Finding)
      Approach: set the variables to command line argument. followed directions. Used formula. Got the desired output.

      Issues and resolution: no issues

   Problem 5 (Greatest Common Divisor)
      Approach: set the variables to command line argument. followed the directions. Used while statement. Got solution.

      Issues and resolution: no issues

   Problem 6 (Fibonacci Number)
      Approach:  set the variables to command line argument. followed the directions. Used while statement.Got solution.

      Issues and resolution: no issues

   Problem 7 (Sum of Powers)
      Approach:  set the variables to command line argument. followed the directions. Used for statement. Got solution.

      Issues and resolution: no issues

   Problem 8 (Dragon Curve)
      Approach: set the variables to command line argument. followed the directions. Used for statement. Got solution.

      Issues and resolution: no issues

   Problem 9 (Perfect Numbers)
      Approach: set the variables to command line argument. followed the directions. Used for statement. Got solution.

      Issues and resolution: no issues

   Problem 10 (Ramanujan Numbers)
      Approach: set the variables to command line argument. Used four while loops with if statements. got the output.

      Issues and resolution: took me like an hour to figure out the problem. looked at the slide and found the solution.

